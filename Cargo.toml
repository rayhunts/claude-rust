[workspace]
members = [
    "cc-errors",
    "cc-types",
    "cc-tools",
    "cc-provider",
    "cc-engine",
    "cc-server",
    "cc-cli",
    "cc-auth",
    "cc-permission",
    "cc-commands",
    "cc-memory",
]
resolver = "3"

[workspace.dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
axum = "0.8"
reqwest = { version = "0.12", features = ["stream", "json"] }
futures = "0.3"
async-trait = "0.1"
anyhow = "1"
thiserror = "2"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
regex = "1"

claude-rust-errors = { path = "cc-errors" }
claude-rust-types = { path = "cc-types" }
claude-rust-tools = { path = "cc-tools" }
claude-rust-provider = { path = "cc-provider" }
claude-rust-engine = { path = "cc-engine" }
claude-rust-server = { path = "cc-server" }
claude-rust = { path = "cc-cli" }
claude-rust-auth = { path = "cc-auth" }
claude-rust-permission = { path = "cc-permission" }
claude-rust-commands = { path = "cc-commands" }
claude-rust-memory = { path = "cc-memory" }
